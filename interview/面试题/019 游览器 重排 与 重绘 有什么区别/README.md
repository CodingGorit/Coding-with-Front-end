# 重排 与 重绘

## 重排

回流：布局引擎会根据所有样式计算出盒模型在页面上的位置 和 大小

## 重绘

计算好盒模型的位置、大小和其他一些属性，游览器就会根据每个盒模型特征进行绘制

## 游览器渲染机制

对 DOM 的大小，位置进行修改后，游览器需要重新计算元素的这些几何属性，叫重排
对 DOM 的样式进行修改，比如 color，background-color，游览器不需要进行几何算的时候，直接绘制了该元素的新样式，那么这里只发生了重绘